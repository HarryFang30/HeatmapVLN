---
description: Git commit规则 - 禁止co-author，保持提交简洁
alwaysApply: true
---

# Git Commit 规则

## 禁止使用 Co-author

- **严格禁止**在commit时使用 `--trailer` 参数添加 Co-authored-by
- **严格禁止**在 commit message 中添加任何形式的 co-author 信息
- commit 的作者就是用户本人，不要添加 AI 或其他人作为共同作者

## Commit 格式要求

- 保持 commit message 简洁明了
- 只包含变更描述，不添加额外的 trailer 或 footer
- 使用中文描述变更内容

## 示例

```bash
# ✅ 正确的做法
git commit -m "修复用户登录bug"

# ❌ 错误的做法 - 禁止使用 --trailer
git commit -m "修复用户登录bug" --trailer "Co-authored-by: AI <ai@example.com>"

# ❌ 错误的做法 - 禁止在message中添加co-author
git commit -m "修复用户登录bug

Co-authored-by: AI <ai@example.com>"
```
